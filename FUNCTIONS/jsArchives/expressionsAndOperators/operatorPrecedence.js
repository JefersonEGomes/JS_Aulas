/* Operator precedence
    Precedência de operadores

    * grouping                  ( )
    * negação a incremento      ! ++ --
    * multiplicação e divisão   * /
    * adição e subtração        + -
    * relacional                < <= > >=
    * igualdade                 == != === !==
    * AND                       &&
    * OR &&                     ||
    * condicional               ?:
    * assignment (atribuição)   = += -= *=

*/

console.log((2 + 5) * 10) // grouping

console.log(3 > 2 == 1 && 2 > 1) // relacional / AND